import React from "react";
import "./Background.css";

const Background = () => (
	<header className = "header">
		<h1>Clicky Game!</h1>
		<h2>Click on any image to earn points, but don't click on any more than once!</h2>
	</header>
);
export default Background;